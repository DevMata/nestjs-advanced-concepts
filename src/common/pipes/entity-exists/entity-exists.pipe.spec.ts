import { EntityExistsPipeCls } from './entity-exists-pipe.service';

describe('EntityExistsPipe', () => {
  it('should be defined', () => {
    expect(new EntityExistsPipeCls()).toBeDefined();
  });
});
